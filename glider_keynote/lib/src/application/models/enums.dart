enum MouseButton {
  left,
  middle,
  right,
}

enum KeyboardModifier {
  alt,
  control,
  shift,
}

enum KeynoteCommandTopic {
  mouse,
  keyboard,
}

enum KeynoteMouseCommandType {
  click,
  move,
  offset,
}
